### T001 - Reset Docs To Current Execution Baseline

#### Objective
Refresh repository documentation so future agents and humans start from the real current state: synthetic and local CSV scatter/timeline density, brushing, evidence export, and manifests already exist.

#### Scope
- Edit `docs/AGENTS.md` to replace the stale `Current Next Task` and `Suggested Future Prompt` sections.
- Edit `docs/MVP.md` to clarify that Milestone 6 is partly started through v1 JSON/Markdown evidence exports but not complete as a report system.
- Edit `README.md` only if the current-state summary or deferred-work list needs minor alignment with the new roadmap.
- Do not change Rust source, tests, manifests, schemas, or lockfiles.

#### Out of Scope
- Do not implement any roadmap feature.
- Do not create a new product spec outside this planning bundle.
- Do not rename milestones or erase useful historical milestone notes.
- Do not claim production readiness, benchmarked performance, GPU row-id preservation, row tables, screenshots, Parquet, or egui support.

#### Blocked By
None

#### Why It Is Blocked
This task can start immediately because it only reconciles docs with code and existing docs.

#### Related Code
- `docs/AGENTS.md` - stale `Current Next Task` still points at Milestone 1.
- `docs/MVP.md` - detailed milestone history through Milestone 5A and Milestone 6 heading.
- `README.md` - current supported/deferred feature summary.
- `docs/ARCHITECTURE.md` - current Milestone 3/4F architecture summary and deferred work.

#### Implementation Touchpoints
- Read first: `AGENTS.md`, `docs/AGENTS.md`, `README.md`, `docs/MVP.md`, `docs/ARCHITECTURE.md`, and this roadmap index.
- Edit first: `docs/AGENTS.md`.
- Symbols to search: `Current Next Task`, `Suggested Future Prompt`, `Milestone 6`, `Evidence export note`, `Deferred Work`.
- Required structural changes: replace stale next-task language with the next recommended execution arc: dataset identity, generic records, local row retention, evidence v2, CPU-backed drilldown.
- Do not edit: crate source files, schema v1 docs except for typo-level corrections, `Cargo.toml`, `Cargo.lock`.
- Verify in order: `git diff -- docs/AGENTS.md docs/MVP.md README.md`; optional `cargo test --workspace` only if any Rust-facing example command was changed.
- Stop and ask: if the user wants milestone numbers preserved exactly while also making roadmap tasks milestone-like.

#### Implementation Details
- In `docs/AGENTS.md`, replace:
```markdown
## Current Next Task

Build deterministic synthetic point/event datasets and a CPU-side density reference implementation for testing future GPU output.
```
with:
```markdown
## Current Next Task

Execute the RawScope roadmap baseline: separate synthetic-only concepts from source-aware dataset identity, retain local row evidence, publish evidence schema v2, and add CPU-backed row drilldown before broad UI, Parquet, benchmarks, or plugin work.
```
- In `docs/AGENTS.md`, replace the suggested prompt with:
```markdown
## Suggested Future Prompt

Implement the next unblocked RawScope roadmap task from `features/rawscope-execution-roadmap/tasks.md`. Keep RawScope local-first and evidence-centered; do not add DataFusion, Tauri, WASM, cloud workflows, plugins, Parquet/Arrow, or benchmark claims unless that specific task calls for them.
```
- In `docs/MVP.md`, under `Milestone 6: First Evidence Export`, add a status note:
```markdown
- Status note: Scatter and timeline v1 JSON/Markdown selection evidence exports exist for cached CPU-side evidence. The remaining Milestone 6 work is a production evidence-report workflow with source-aware dataset identity, view configuration, selected source rows, and rendered visual context.
```
- If `README.md` needs alignment, keep changes to one or two bullets in `Deferred Work`.
- Do not create a `docs/ROADMAP.md` in this task; this planning bundle is the roadmap source for now.

#### Before / After
**Before:**
```markdown
## Current Next Task

Build deterministic synthetic point/event datasets and a CPU-side density reference implementation for testing future GPU output.
```

**After:**
```markdown
## Current Next Task

Execute the RawScope roadmap baseline: separate synthetic-only concepts from source-aware dataset identity, retain local row evidence, publish evidence schema v2, and add CPU-backed row drilldown before broad UI, Parquet, benchmarks, or plugin work.
```

#### Migration Notes
- No API migration.
- Existing docs references to completed milestones should remain useful history.
- Existing tests should pass unchanged.

#### Misinterpretations To Prevent
- Wrong interpretation: "Update docs by declaring the roadmap complete."
  Correct rule: docs must say the roadmap is future work.
- Wrong interpretation: "Rewrite the whole product vision."
  Correct rule: preserve the existing vision and only correct stale execution status.
- Wrong interpretation: "Add Parquet/egui/benchmark work because they are listed in deferred work."
  Correct rule: this task is documentation-only.

#### Invariants
- `docs/AGENTS.md` must no longer tell agents that Milestone 1 is the current next task.
- The docs must still preserve RawScope's non-goals.
- The docs must not imply benchmarked performance, production report workflows, GPU row-id preservation, or generic BI/dashboard scope.

#### Acceptance Criteria
- [ ] `docs/AGENTS.md` names the roadmap baseline as the current next execution arc.
- [ ] `docs/MVP.md` distinguishes existing v1 evidence exports from the unfinished production report workflow.
- [ ] `git diff` shows documentation-only changes.

#### Testing Guidance
- No Rust tests are required if only docs change.
- Run `cargo test --workspace` if command examples or Rust-facing docs are materially changed.

#### Agent Handoff Context
This is the cleanup task that prevents future agents from starting at an already-completed milestone. It should be small and boring. Preserve the historical milestone notes because they explain why the current architecture exists.

## Final Notes

- Recommended implementation order: first.
- Unresolved questions: none.
- Risks: over-editing docs into a new product strategy instead of correcting stale status.
- Areas that need human review before implementation: only if milestone numbering should be restructured.
